import React from 'react'

export default function SectionWrapper({ className = '' }) {
          return (
                    <div className={`py-3 ${className}`}>
                              <div className="mx-auto max-w-7xl space-y-6 sm:px-6 lg:px-8">
                              </div>
                    </div>
          )
}
